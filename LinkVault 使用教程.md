# LinkVault 使用教程

## 目录

1. [简介](#1-简介)
2. [书签管理](#2-书签管理)
3. [文件夹组织](#3-文件夹组织)
4. [标签系统](#4-标签系统)
5. [搜索与查找](#5-搜索与查找)
6. [快速捕获书签](#6-快速捕获书签)
7. [点击统计与排序](#7-点击统计与排序)
8. [定时任务](#8-定时任务)
9. [随机漫步](#9-随机漫步)
10. [备份与恢复](#10-备份与恢复)
11. [回收站](#11-回收站)
12. [界面主题](#12-界面主题)
13. [快捷键与设置](#13-快捷键与设置)

---

## 1. 简介

LinkVault 是一款桌面书签管理工具，帮助您高效地收藏、组织和查找网址。支持文件夹分类、标签标注、全局快捷键捕获、定时任务、随机漫步等功能。

**启动方式**：双击 `LinkVault.exe` 

---

## 2. 书签管理

### 添加书签

- **手动添加**：点击首页工具栏的 **+ 添加书签** 按钮，填写标题、网址、备注、标签等信息后保存。
- **快速捕获**：在任何页面按下 `Ctrl+Shift+N`（默认），自动从剪贴板填入网址，快速保存书签。

### 编辑书签

在书签列表中选中某条书签，点击 **编辑** 按钮，或右键选择 **编辑**，修改信息后保存。

### 删除书签

选中书签后点击 **删除**，书签将移入回收站，可在回收站中恢复或永久删除。

### 批量操作

按住 `Ctrl` 点击多选书签，然后使用工具栏的批量功能：

- **批量移动**：将选中的书签移到指定文件夹
- **批量标签**：为选中的书签统一添加标签
- **批量浏览器**：修改选中书签的默认打开浏览器
- **批量删除**：将选中的书签移入回收站

---

## 3. 文件夹组织

左侧面板显示文件夹树形结构。

### 操作

- **新建文件夹**：右键左侧空白区域或文件夹，选择 **新建文件夹**
- **重命名**：选中文件夹，选择 **重命名**
- **删除**：删除文件夹不会删除其中的书签
- **展开/折叠**：点击文件夹前的箭头

点击某个文件夹，右侧书签列表会自动筛选显示该文件夹下的书签。

---

## 4. 标签系统

书签可以添加多个标签，用逗号分隔（如：`工作, 技术, 待读`）。

### 按标签筛选

首页工具栏下方会显示所有标签按钮，点击某个标签即可筛选出带该标签的所有书签。再次点击取消筛选。

---

## 5. 搜索与查找

首页顶部搜索框支持全局搜索，匹配书签的标题、网址、备注和标签名称。

- 输入关键词后按回车或点击搜索图标
- 清空搜索框或点击右侧 × 按钮返回完整列表

---

## 6. 快速捕获书签

全局快捷键 `Ctrl+Shift+N`（可自定义）唤出快速添加弹窗。

### 功能特点

- 自动检测剪贴板中的网址并填入
- 支持选择目标文件夹
- 支持填写标签
- 按 `Enter` 保存，按 `Esc` 取消
- 保存后首页列表自动刷新

### 修改快捷键

打开 **文件 > 设置 > 热键**，点击"快速捕获书签"输入框，按下新快捷键后点击 **应用**。

---

## 7. 点击统计与排序

每个书签会自动记录：

- **点击次数**：通过 LinkVault 打开网址的次数
- **最近打开时间**：最后一次通过 LinkVault 打开的时间

### 排序方式

首页工具栏下方有排序下拉框，支持三种排序：

- **默认（标题）**：按标题字母排序
- **最常打开**：按点击次数从高到低排序
- **最近打开**：按最近打开时间从新到旧排序

### 重置统计

点击 **重置统计** 按钮可将所有书签的点击次数和打开时间清零。

---

## 8. 定时任务

定时任务标签页（定时任务）用于设置定时打开书签的计划。

### 新建任务

1. 点击 **+ 新建任务**
2. 填写任务名称
3. 设置执行时间（时:分）
4. 选择重复模式（每天、工作日、仅一次等）
5. 从书签列表中选择要打开的书签

### 管理任务

- **编辑**：修改已有任务
- **删除**：删除任务
- **启用/禁用**：切换任务是否执行

---

## 9. 随机漫步

随机池标签页（随机池）用于创建书签池，每次随机打开池中的一个书签。

### 操作步骤

1. 点击 **+ 新建池**，输入池名称
2. 在首页书签列表点击 **池** 列（☐），书签变为 ☑ 表示已加入当前活跃池
3. 在随机池页面点击 **设为活跃** 选择一个活跃池
4. 在底部随机漫步栏点击 **漫步**，系统随机打开池中一个书签

### 管理池

- **删除池**：选中池后点击 **删除池**
- **切换活跃池**：选中池后点击 **设为活跃**

---

## 10. 备份与恢复

### 导出备份

**文件 > 导出备份** 或 **设置 > 导出备份**

- 支持按**文件夹**和**标签**筛选要导出的内容
- 备份格式为 `.linkvault` 加密文件

### 导出 HTML

**文件 > 导出 HTML...** 导出标准 HTML 书签文件，可在浏览器中导入使用。

### 导入备份

**文件 > 导入备份...** 选择 `.linkvault` 备份文件恢复书签。

---

## 11. 回收站

删除的书签移入回收站标签页（回收站），不会立即永久删除。

### 操作

- **恢复**：选中书签，点击 **恢复**，书签回到原文件夹
- **彻底删除**：选中书签，点击 **彻底删除**，不可恢复
- **清空回收站**：点击 **清空回收站**，删除所有回收站中的书签，不可恢复

---

## 12. 界面主题

LinkVault 支持 9 种界面主题，打开 **文件 > 设置 > 常规** 即可切换。

### 可用主题

| 主题 | 类型 |
|------|------|
| 浅色蓝 | 浅色 |
| 浅色青 | 浅色 |
| 浅色绿 | 浅色 |
| 浅色粉 | 浅色 |
| 深色蓝 | 深色 |
| 深色青 | 深色 |
| 深色绿 | 深色 |
| 深色粉 | 深色 |
| 深色琥珀 | 深色 |

选择主题后立即生效。

---

## 13. 快捷键与设置

### 全局快捷键

| 功能 | 默认快捷键 | 说明 |
|------|-----------|------|
| 全局搜索 | `Ctrl+Shift+L` | 在任何页面唤出搜索弹窗 |
| 快速捕获 | `Ctrl+Shift+N` | 在任何页面唤出快速添加书签弹窗 |

快捷键可在 **文件 > 设置 > 热键** 中自定义修改。

### 其他设置

- **开机自启**：设置软件随 Windows 开机自动启动
- **最小化到托盘**：关闭窗口时最小化到系统托盘

---

> **提示**：如果在使用过程中遇到问题，检查书签列表底部的状态提示，或尝试重启软件。